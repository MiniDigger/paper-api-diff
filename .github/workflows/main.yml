name: Build
on: 
  workflow_dispatch:

jobs:
  job:
     runs-on: ubuntu-latest
     steps: 
     - name: Checkout
       uses: actions/checkout@v1
     - name: Setup Java
       uses: actions/setup-java@v1
       with:
         java-version: '8'
     - name: Custom
       uses: ./.github/actions/custom
